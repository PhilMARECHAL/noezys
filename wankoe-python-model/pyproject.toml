[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "wankoe-model"
version = "0.1.0"
description = "Wankoe Python Model — modèle Python pour les projets Noezys"
readme = "README.md"
requires-python = ">=3.10"
authors = [
    { name = "Noezys" },
]
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
