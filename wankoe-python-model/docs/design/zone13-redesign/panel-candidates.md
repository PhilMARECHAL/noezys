# Panel round 1 — CANDIDATE CIRCUITS (2026-08-14, commit d82a679)

Feed fact: FeedLime 6/20 has NO native 2-4 (zone 1.2 cuts at 6 mm) and no
fines; the 6/20 double cut insulates zone 1.3 from feed quality (both D4
feeds give identical results — robustness is structural).

C1 (BUILD recommendation) — two-stage smooth double-rolls + one triple-deck
grading screen (8 / 3.75 / 2 mm) with immediate 2-4 relief + 1.5 mm sliver
deck, sliver NOT reground: ratio 0.98-1.05 (worst-sensitivity 1.13, all
< 1.25 across the honest [H] band), grits QC 3.0 % >4 / 5.2 % <2, 5 units,
phase-1 turndown = run 1 of the 2 parallel RC-2 rolls. 80 kt at 88.6-91.5 %
of hours ceiling; dryer at rating.
C2 — three-stage extra-gentle (ratio/pass ~1.7): ratio 0.84 (0.99 at n 1.6),
7 units; the escalation if the vendor test returns n < 1.7.
C3 — topology fix with toothed rolls kept: ratio 2.90 — PROOF the machine,
not the circuit, is the root cause. Rejected.
C4 — quad-roll compact (2 units): 1.10 but 1.30 at S_att 0.10 — crosses the
firm limit on a plausible coefficient. Capex fallback only.
C5 — low-speed impactor stage 1: 1.09 but 1.30 at honest n 1.0; blowbar
opex at 7500 h. Marginal.

Machine spec (purchase-grade, from both reports): final-stage crusher must
place >= 40 % of crushed product in 2-4 mm with <= 29 % < 1.5 mm per pass
(RR n >= 1.6 at x80 ~3.5-4); smooth double-rolls at reduction <= 2-2.5 are
the only credible class [H — vendor gradation test on WANKOE 6/20 = trial
trigger #1, with roll capacity at 3.4 mm gap].

Open arbitrations: sliver outlet at 80 kt (~22 kt/y of 1.5-2 with no home:
widen fines to 0/2? meter <= 1.68 t/h into grits within D6? cut at 1.8 mm?);
engine wiring needs a per-machine m7_n_comp slot (one-line, pattern exists).
(Full report in session log 2026-08-14.)
